/**
 * Save the clipboard.
 */
export default class {
    /**
     * Copy the string to the clipboard.
     *
     * @param  {string}        str
     * @return {Promise<void>}
     */
    static save(str: string): Promise<void>;
}
